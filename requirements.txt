# Naruto Kayou Price Checker v9

Fixed: `NameError: name 'theme' is not defined`

## Streamlit Cloud
- Main file path: `app.py`
- Python version: `3.11`

## Show card images
Images can show when using:
1. Manual Sold CSV with column `image_url`
2. eBay Active API with Bearer Token

Search Link Only opens market links only and does not pull images back into the app.
